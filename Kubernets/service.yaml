apiVersion: v1
kind: Service
metadata:
  name: expense-tracker-webapp-service
 # namespace: tracker
spec:
  selector:
    app: expense-tracker-webapp
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
      
